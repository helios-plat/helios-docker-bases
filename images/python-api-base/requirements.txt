fastapi==0.136.*
uvicorn[standard]==0.34.*
asyncpg==0.31.*
pydantic==2.13.*
pydantic-settings==2.7.*
uvloop==0.21.*
httpx==0.28.*
python-json-logger==2.0.*
# orjson removed: PyO3 0.23 (used by orjson 3.10) does not support Python 3.14

# 3O stack — every private package must be pinned to its git ref: uv has no
# private index, so transitive private deps (obase, required by omodul) are not
# discoverable and must be listed explicitly or resolution fails.
obase @ git+https://github.com/helios-plat/obase.git@v0.15.12
oprim @ git+https://github.com/helios-plat/oprim.git@v3.19.1
oskill @ git+https://github.com/helios-plat/oskill.git@v4.0.0
omodul @ git+https://github.com/helios-plat/omodul.git@v1.30.10
